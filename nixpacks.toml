[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
cmds = ["npm ci || npm install"]

[phases.build]
cmds = ["echo 'Skipping build for debugging'"]

[start]
cmd = "npm run start:simple"

[variables]
PORT = "5000"